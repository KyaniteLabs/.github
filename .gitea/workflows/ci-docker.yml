name: Docker Build
on:
  push:
    branches: [main]
    tags: ["v*"]
jobs:
  build:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4
      - name: Build Docker image
        run: docker build -t test .
